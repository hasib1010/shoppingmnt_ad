// worker.js
const { parentPort, workerData } = require('worker_threads');
const { test } = require('./trnsearch')
// const { test } = require('./optimizing')
const end = workerData;
let sum = 0;

for (let i = 0; i <= end; i++) {
  sum += i;
}

setTimeout(()=>{
  parentPort.postMessage(sum);
}, 5000)

